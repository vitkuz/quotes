extends layout

block navigation
    div
        a(href='/users/login') Login
        a(href='/users/register') Register
        a(href='/users/password') Remember password
        a(href='/users/logout') Logout
        a(href='/profile') Profile


block content
    .container
        each duplicate in page.result
            div.quote
                p
                    strong
                    |  - #{duplicate._id.text}
            div
                each did in duplicate.uniqueIds
                    span
                        #{did}



